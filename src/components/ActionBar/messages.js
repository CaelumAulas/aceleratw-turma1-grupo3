import { defineMessages } from 'react-intl';

const prefix = 'components.ActionBar';

export default defineMessages({
  deleteButton: {
    id: `${prefix}.deleteButton`,
    defaultMessage: 'Deletar todos',
  },
  addButton: {
    id: `${prefix}.addButton`,
    defaultMessage: 'Adicionar',
  },
});
